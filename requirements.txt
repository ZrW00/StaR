torch==2.6.0
torchvision==0.21.0
transformers==4.51.1
deepspeed==0.15.4
flash_attn==2.7.4.post1
zhipuai
json5
qwen_vl_utils


# LLaMA-Factory, we provide the reference of the source code of 0.9.4.dev0 version, cd to the directory of LLaMA-Factory and run the following command to install the dependencies: `pip install -e ".[torch,metrics]" --no-build-isolation`